{
  "name": "@spark/starter",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "The smallest complete Spark Engine app: one scene, a player, a level, a sound. Copy it with `pnpm create-app <name>`.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@spark/engine": "workspace:*",
    "three": "0.185.1"
  },
  "devDependencies": {
    "@types/three": "0.185.0",
    "@webgpu/types": "0.1.72"
  }
}
